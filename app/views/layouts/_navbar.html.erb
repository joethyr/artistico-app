<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #475C4D;">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Artistico</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="navbar-collapse d-flex justify-content-between">
      <div class="navbar-nav">
        <%= link_to "Pictures", root_path, class:"nav-link", id: "nav-link-color" %>
      </div>
      <% if user_signed_in? %>
        <div class="navbar-nav">
          <%= link_to "Upload Picture", new_picture_path, class:"nav-link", id: "nav-link-color" %>
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, class:"nav-link", id: "nav-link-color" %>
        </div>
      <% else %>
        <div class="navbar-nav ">
          <%= link_to "Log In", new_user_session_path, class:"nav-link", id: "nav-link-color" %>
          <%= link_to "Sign Up", new_user_registration_path, class:"nav-link", id: "nav-link-color" %>
        </div>
      <% end %>
    </div>
  </div>
</nav>
